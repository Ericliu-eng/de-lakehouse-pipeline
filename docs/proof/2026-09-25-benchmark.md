# Pipeline Benchmark — 2026-09-25

Generated by `python -m scripts.benchmark`. Saved Alpha Vantage payloads were replayed into a dedicated PostgreSQL database, so no API calls were made and the development database was not modified. Timings are single-machine local measurements, not production SLAs.

| Environment | Value |
| --- | --- |
| Commit | `e79b8c4 (uncommitted changes)` |
| Python / PostgreSQL | 3.13.12 / 16.12 (Debian 16.12-1.pgdg13+1) |
| OS | Windows 11 |
| Input | 10 symbols (AAPL, AMZN, GOOGL, JNJ, JPM, META, MSFT, NVDA, WMT, XOM); latest saved payload per symbol |
| Skipped (< 20 rows) | SPCX |

## Headline results

| Metric | Result |
| --- | --- |
| Warehouse size | 1,000 rows · 10 symbols · 2026-04-28 to 2026-09-18 |
| Rerun of identical payloads | 0 new rows, 0 duplicate keys |
| Incremental run | 50 of 1,000 staged rows loaded (5 new days × 10 symbols) |
| Atomic rollback | no partial writes in 3 tables after a rejected audit write |
| Backfill resume after crash | 0 missing trading days, 0 duplicates |
| Quality gate | 2 of 2 injected bad rows caught; 0 reached marts |
| End-to-end run (ingest → quality → marts) | median 171 ms (min 135 ms, max 234 ms) per symbol |
| Full 10-symbol batch | median 1,735 ms (min 1,608 ms, max 1,880 ms) |

## 1. Incremental loading and idempotency

Each symbol was first loaded without its newest 5 trading days, then with the full payload, then with the full payload again.

| Run | Rows staged | Rows written | Audit rows | Time | Check |
| --- | ---: | ---: | ---: | ---: | --- |
| Initial | 950 | 950 | 10 | 858 ms | — |
| Incremental | 1,000 | 50 | 10 | 977 ms | watermark advanced for 10/10 symbols: pass |
| Rerun | 1,000 | 0 | 0 | 550 ms | watermark unchanged for 10/10 symbols: pass |

Duplicate `(ts, symbol)` groups after all runs: **0**.

## 2. Transactional rollback

A CHECK constraint rejected the `load_metadata` insert for AAPL, after the fact upsert and watermark update had executed in the same transaction.

| Table | Before | After failed run | After retry |
| --- | ---: | ---: | ---: |
| `market_bars` | 99 | 99 | 100 |
| `pipeline_metadata` | 1 | 1 | 1 |
| `load_metadata` | 1 | 1 | 2 |

Error raised: `CheckViolation`. Watermark unchanged after failure: pass. Watermark after retry: 2026-09-18.

## 3. Backfill crash and resume

Backfill of AAPL for 2026-09-05 to 2026-09-18 (14 calendar days, 9 trading days) was forced to fail after 4 trading days had loaded, then rerun.

| Metric | Result |
| --- | ---: |
| Trading days loaded before crash | 4 |
| Dates processed on resume | 10 (5 trading days; weekends are retried because they are never marked complete) |
| Resume time | 708 ms |
| API payload fetches (first run, resume) | 1, 1 |
| Missing trading days after resume | 0 |
| Duplicate keys | 0 |
| Trading dates reprocessed by a third, no-op run | 0 |

## 4. Quality gate

Clean data: 60/60 checks passed (6 per symbol); 10/10 orchestrated runs succeeded. Mart rows built: `mart_daily_symbol_summary` 1,000, `mart_symbol_latest_price` 10, `mart_symbol_volume_rank` 1,000.

Injected 2 bad rows (one negative close, one negative volume). The orchestrated run ended with status `failed`:

| Step | Status |
| --- | --- |
| run_stock_pipeline | success |
| run_quality_checks | failed |
| build_marts | not run |

Failed checks:

- `range`: Column close has 1 row(s) outside range min=0, max=None.
- `range`: Column volume has 1 row(s) outside range min=0, max=None.

Bad rows present in marts: **0**.

## 5. API retry behavior

Scripted HTTP responses drove the real `fetch_json_with_retry` loop (max 3 retries). Backoff delays were recorded instead of slept.

| Scenario | Attempts | Outcome | Backoff (s) |
| --- | ---: | --- | --- |
| 429, 503, then 200 | 3 | success | 1, 2 |
| 200 with throttle note, then 200 | 2 | success | 1 |
| Connection error, then 200 | 2 | success | 1 |
| 429 on every attempt | 4 | raised HTTPError | 1, 2, 4 |
| 400 bad request | 1 | raised HTTPError | — |

## 6. End-to-end latency

`run_orchestrated_pipeline` was run for each of 10 symbols from an empty warehouse, 5 times (50/50 runs succeeded).

| Scope | Duration |
| --- | --- |
| 10-symbol batch | median 1,735 ms (min 1,608 ms, max 1,880 ms) |
| One symbol, all steps | median 171 ms (min 135 ms, max 234 ms) |
| Step `run_stock_pipeline` | median 91 ms (min 76 ms, max 132 ms) |
| Step `run_quality_checks` | median 32 ms (min 20 ms, max 65 ms) |
| Step `build_marts` | median 45 ms (min 24 ms, max 80 ms) |
